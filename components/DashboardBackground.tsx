import { SafeAreaView, Text } from "react-native";

const DashboardBackground = ({children}: any) => {
    return (
        <SafeAreaView>
            {children}
        </SafeAreaView>
    )
}

export default DashboardBackground;